package thistype.pkg.abstracts.joinpoints;

import org.lara.interpreter.weaver.interf.*;
import thistype.pkg.ThisTypeWeaver;
import java.lang.Object;
import thistype.pkg.entities.*;
import thistype.pkg.enums.*;
import org.lara.interpreter.exception.ActionException;
import org.lara.interpreter.exception.AttributeException;
import org.lara.interpreter.weaver.interf.events.Stage;
import java.util.Optional;

import java.util.*;
import java.util.function.IntFunction;

/**
 * Auto-generated abstract class for the {@code joinpoint} join point.
 * <p>
 * This class is generated by WeaverGen2 and should not be edited manually.
 */
public abstract class AJoinpoint<Self extends AJoinpoint<Self>> extends JoinPoint2<Self, AJoinpoint<?>> {

    /**
     *  FIXME: This should be a private field
     */ 
    @Deprecated
    protected Object node;

    public AJoinpoint(Object node, ThisTypeWeaver weaver) {
        super(weaver);
        this.node = node;
    }

    public Object getNodeImpl() {
        return node;
    }

    public abstract Self getRootImpl();

    public abstract Self[] getAncestorsImpl();

    @Deprecated
    public final Object[] ancestors() {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAttribute(Stage.BEGIN, this, "ancestors", Optional.empty());
            }
            Self[] result = this.getAncestorsImpl();
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAttribute(Stage.END, this, "ancestors", Optional.ofNullable(result));
            }
            return result;
        } catch (Exception e) {
            throw new AttributeException(get_class(), "ancestors", e);
        }
    }

    public abstract List<Self> getSiblingsImpl();

    @Deprecated
    public final Object siblings() {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAttribute(Stage.BEGIN, this, "siblings", Optional.empty());
            }
            List<Self> result = this.getSiblingsImpl();
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAttribute(Stage.END, this, "siblings", Optional.ofNullable(result));
            }
            return result;
        } catch (Exception e) {
            throw new AttributeException(get_class(), "siblings", e);
        }
    }

    public abstract Self selfTransformImpl(String transformName);

    @Deprecated
    public final Object selfTransform(String transformName) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "selfTransform", Optional.empty(), transformName);
            }
            Self result = this.selfTransformImpl(transformName);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "selfTransform", Optional.ofNullable(result), transformName);
            }
            return result;
        } catch (Exception e) {
            throw new ActionException(get_class(), "selfTransform", e);
        }
    }

    public abstract void replaceWithImpl(Self replacement);

    @Deprecated
    public final void replaceWith(Object replacement) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "replaceWith", Optional.empty(), replacement);
            }
            this.replaceWithImpl((Self) replacement);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "replaceWith", Optional.empty(), replacement);
            }
            return;
        } catch (Exception e) {
            throw new ActionException(get_class(), "replaceWith", e);
        }
    }

    public abstract List<Self> findAllImpl(String pattern);

    @Deprecated
    public final Object findAll(String pattern) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "findAll", Optional.empty(), pattern);
            }
            List<Self> result = this.findAllImpl(pattern);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "findAll", Optional.ofNullable(result), pattern);
            }
            return result;
        } catch (Exception e) {
            throw new ActionException(get_class(), "findAll", e);
        }
    }

    public abstract Map<String, Self> categorizeImpl();

    @Deprecated
    public final Object categorize() {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "categorize", Optional.empty());
            }
            Map<String, Self> result = this.categorizeImpl();
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "categorize", Optional.ofNullable(result));
            }
            return result;
        } catch (Exception e) {
            throw new ActionException(get_class(), "categorize", e);
        }
    }

    @Override
    public String get_class() {
        return "joinpoint";
    }

    @Override
    public boolean getInstanceOfImpl(String joinpointClassname) {
        return "joinpoint".equals(joinpointClassname);
    }

    @SuppressWarnings("unchecked")
    @Override
    protected IntFunction<Self[]> selfTypeArrayFactory() {
        return size -> (Self[]) new AJoinpoint[size];
    }

    @Override
    protected final IntFunction<AJoinpoint<?>[]> jpTypeArrayFactory() {
        return size -> new AJoinpoint[size];
    }
}
