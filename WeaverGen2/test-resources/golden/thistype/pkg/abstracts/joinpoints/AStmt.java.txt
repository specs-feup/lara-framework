package thistype.pkg.abstracts.joinpoints;

import org.lara.interpreter.weaver.interf.*;
import thistype.pkg.ThisTypeWeaver;
import thistype.pkg.joinpoints.ThisTypeNode;
import java.lang.Object;
import thistype.pkg.entities.*;
import thistype.pkg.enums.*;
import org.lara.interpreter.exception.ActionException;
import org.lara.interpreter.weaver.interf.events.Stage;
import java.util.Optional;

import java.util.*;
import java.util.function.IntFunction;

/**
 * Auto-generated abstract class for the {@code stmt} join point.
 * <p>
 * This class is generated by WeaverGen2 and should not be edited manually.
 */
public abstract class AStmt<Self extends AStmt<Self>> extends ThisTypeNode<Self> {

    public AStmt(Object node, ThisTypeWeaver weaver) {
        super(node, weaver);
    }

    public abstract Self moveAfterImpl(Self target);

    @Deprecated
    public final Object moveAfter(Object target) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "moveAfter", Optional.empty(), target);
            }
            Self result = this.moveAfterImpl((Self) target);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "moveAfter", Optional.ofNullable(result), target);
            }
            return result != null ? result : getUndefinedValue();
        } catch (Exception e) {
            throw new ActionException(get_class(), "moveAfter", e);
        }
    }

    public abstract Self extractToImpl(String context);

    @Deprecated
    public final Object extractTo(String context) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "extractTo", Optional.empty(), context);
            }
            Self result = this.extractToImpl(context);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "extractTo", Optional.ofNullable(result), context);
            }
            return result != null ? result : getUndefinedValue();
        } catch (Exception e) {
            throw new ActionException(get_class(), "extractTo", e);
        }
    }

    @Override
    public String get_class() {
        return "stmt";
    }

    @Override
    public boolean instanceOfImpl(String joinpointClassname) {
        return "stmt".equals(joinpointClassname)
            || "node".equals(joinpointClassname)
            || "joinpoint".equals(joinpointClassname);
    }

    @SuppressWarnings("unchecked")
    @Override
    protected IntFunction<Self[]> selfTypeArrayFactory() {
        return size -> (Self[]) new AStmt[size];
    }
}
