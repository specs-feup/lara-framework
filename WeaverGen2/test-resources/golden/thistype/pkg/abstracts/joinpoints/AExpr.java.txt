package thistype.pkg.abstracts.joinpoints;

import org.lara.interpreter.weaver.interf.*;
import thistype.pkg.ThisTypeWeaver;
import thistype.pkg.joinpoints.ThisTypeNode;
import java.lang.Object;
import thistype.pkg.entities.*;
import thistype.pkg.enums.*;
import org.lara.interpreter.exception.ActionException;
import org.lara.interpreter.weaver.interf.events.Stage;
import java.util.Optional;

import java.util.*;
import java.util.function.IntFunction;

/**
 * Auto-generated abstract class for the {@code expr} join point.
 * <p>
 * This class is generated by WeaverGen2 and should not be edited manually.
 */
public abstract class AExpr<Self extends AExpr<Self>> extends ThisTypeNode<Self> {

    public AExpr(Object node, ThisTypeWeaver weaver) {
        super(node, weaver);
    }

    public abstract Self evaluateImpl();

    @Deprecated
    public final Object evaluate() {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "evaluate", Optional.empty());
            }
            Self result = this.evaluateImpl();
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "evaluate", Optional.ofNullable(result));
            }
            return result;
        } catch (Exception e) {
            throw new ActionException(get_class(), "evaluate", e);
        }
    }

    public abstract Self substituteImpl(Self target, Self replacement);

    @Deprecated
    public final Object substitute(Object target, Object replacement) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "substitute", Optional.empty(), target, replacement);
            }
            Self result = this.substituteImpl((Self) target, (Self) replacement);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "substitute", Optional.ofNullable(result), target, replacement);
            }
            return result;
        } catch (Exception e) {
            throw new ActionException(get_class(), "substitute", e);
        }
    }

    public abstract List<Self> collectTermsImpl();

    @Deprecated
    public final Object collectTerms() {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "collectTerms", Optional.empty());
            }
            List<Self> result = this.collectTermsImpl();
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "collectTerms", Optional.ofNullable(result));
            }
            return result;
        } catch (Exception e) {
            throw new ActionException(get_class(), "collectTerms", e);
        }
    }

    @Override
    public String get_class() {
        return "expr";
    }

    @Override
    public boolean getInstanceOfImpl(String joinpointClassname) {
        return "expr".equals(joinpointClassname)
            || "node".equals(joinpointClassname)
            || "joinpoint".equals(joinpointClassname);
    }

    @SuppressWarnings("unchecked")
    @Override
    protected IntFunction<Self[]> selfTypeArrayFactory() {
        return size -> (Self[]) new AExpr[size];
    }
}
