package thistype.pkg.abstracts.joinpoints;

import org.lara.interpreter.weaver.interf.*;
import thistype.pkg.ThisTypeWeaver;
import thistype.pkg.joinpoints.ThisTypeExpr;
import java.lang.Object;
import thistype.pkg.entities.*;
import thistype.pkg.enums.*;
import org.lara.interpreter.exception.ActionException;
import org.lara.interpreter.weaver.interf.events.Stage;
import java.util.Optional;

import java.util.*;
import java.util.function.IntFunction;

/**
 * Auto-generated abstract class for the {@code binaryExpr} join point.
 * <p>
 * This class is generated by WeaverGen2 and should not be edited manually.
 */
public abstract class ABinaryExpr<Self extends ABinaryExpr<Self>> extends ThisTypeExpr<Self> {

    public ABinaryExpr(Object node, ThisTypeWeaver weaver) {
        super(node, weaver);
    }

    public abstract Self swapOperandsImpl();

    @Deprecated
    public final Object swapOperands() {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "swapOperands", Optional.empty());
            }
            Self result = this.swapOperandsImpl();
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "swapOperands", Optional.ofNullable(result));
            }
            return result != null ? result : getUndefinedValue();
        } catch (Exception e) {
            throw new ActionException(get_class(), "swapOperands", e);
        }
    }

    public abstract void setOperandsImpl(Self left, Self right);

    @Deprecated
    public final void setOperands(Object left, Object right) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "setOperands", Optional.empty(), left, right);
            }
            this.setOperandsImpl((Self) left, (Self) right);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "setOperands", Optional.empty(), left, right);
            }
            return;
        } catch (Exception e) {
            throw new ActionException(get_class(), "setOperands", e);
        }
    }

    @Override
    public String get_class() {
        return "binaryExpr";
    }

    @Override
    public boolean instanceOfImpl(String joinpointClassname) {
        return "binaryExpr".equals(joinpointClassname)
            || "expr".equals(joinpointClassname)
            || "node".equals(joinpointClassname)
            || "joinpoint".equals(joinpointClassname);
    }

    @SuppressWarnings("unchecked")
    @Override
    protected IntFunction<Self[]> selfTypeArrayFactory() {
        return size -> (Self[]) new ABinaryExpr[size];
    }
}
