package thistype.pkg.abstracts.joinpoints;

import org.lara.interpreter.weaver.interf.*;
import thistype.pkg.ThisTypeWeaver;
import thistype.pkg.joinpoints.ThisTypeJoinpoint;
import java.lang.Object;
import thistype.pkg.entities.*;
import thistype.pkg.enums.*;
import org.lara.interpreter.exception.ActionException;
import org.lara.interpreter.weaver.interf.events.Stage;
import java.util.Optional;

import java.util.*;
import java.util.function.IntFunction;

/**
 * Auto-generated abstract class for the {@code container} join point.
 * <p>
 * This class is generated by WeaverGen2 and should not be edited manually.
 */
public abstract class AContainer<Self extends AContainer<Self>> extends ThisTypeJoinpoint<Self> {

    public AContainer(Object node, ThisTypeWeaver weaver) {
        super(node, weaver);
    }

    public abstract List<Map<String, Self>> processNestedImpl();

    @Deprecated
    public final Object processNested() {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "processNested", Optional.empty());
            }
            List<Map<String, Self>> result = this.processNestedImpl();
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "processNested", Optional.ofNullable(result));
            }
            return result;
        } catch (Exception e) {
            throw new ActionException(get_class(), "processNested", e);
        }
    }

    public abstract void addToAllImpl(Map<String, List<Self>> items);

    @Deprecated
    public final void addToAll(Map<String, List<Self>> items) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "addToAll", Optional.empty(), items);
            }
            this.addToAllImpl(items);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "addToAll", Optional.empty(), items);
            }
            return;
        } catch (Exception e) {
            throw new ActionException(get_class(), "addToAll", e);
        }
    }

    @Override
    public String get_class() {
        return "container";
    }

    @Override
    public boolean instanceOfImpl(String joinpointClassname) {
        return "container".equals(joinpointClassname)
            || "joinpoint".equals(joinpointClassname);
    }

    @SuppressWarnings("unchecked")
    @Override
    protected IntFunction<Self[]> selfTypeArrayFactory() {
        return size -> (Self[]) new AContainer[size];
    }
}
