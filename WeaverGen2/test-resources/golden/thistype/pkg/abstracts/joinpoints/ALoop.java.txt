package thistype.pkg.abstracts.joinpoints;

import org.lara.interpreter.weaver.interf.*;
import thistype.pkg.ThisTypeWeaver;
import thistype.pkg.joinpoints.ThisTypeStmt;
import java.lang.Object;
import thistype.pkg.entities.*;
import thistype.pkg.enums.*;
import org.lara.interpreter.exception.ActionException;
import org.lara.interpreter.weaver.interf.events.Stage;
import java.util.Optional;

import java.util.*;
import java.util.function.IntFunction;

/**
 * Auto-generated abstract class for the {@code loop} join point.
 * <p>
 * This class is generated by WeaverGen2 and should not be edited manually.
 */
public abstract class ALoop<Self extends ALoop<Self>> extends ThisTypeStmt<Self> {

    public ALoop(Object node, ThisTypeWeaver weaver) {
        super(node, weaver);
    }

    public abstract List<Self> unrollImpl(Integer factor);

    @Deprecated
    public final Object unroll(Integer factor) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "unroll", Optional.empty(), factor);
            }
            List<Self> result = this.unrollImpl((Integer) factor);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "unroll", Optional.ofNullable(result), factor);
            }
            return result;
        } catch (Exception e) {
            throw new ActionException(get_class(), "unroll", e);
        }
    }

    public abstract Self tileImpl(Integer tileSize);

    @Deprecated
    public final Object tile(Integer tileSize) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "tile", Optional.empty(), tileSize);
            }
            Self result = this.tileImpl((Integer) tileSize);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "tile", Optional.ofNullable(result), tileSize);
            }
            return result != null ? result : getUndefinedValue();
        } catch (Exception e) {
            throw new ActionException(get_class(), "tile", e);
        }
    }

    public abstract void interchangeImpl(Self other);

    @Deprecated
    public final void interchange(Object other) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "interchange", Optional.empty(), other);
            }
            this.interchangeImpl((Self) other);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "interchange", Optional.empty(), other);
            }
            return;
        } catch (Exception e) {
            throw new ActionException(get_class(), "interchange", e);
        }
    }

    public abstract Self fuseImpl(Self other, boolean checkDependencies);

    @Deprecated
    public final Object fuse(Object other, boolean checkDependencies) {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.BEGIN, this, "fuse", Optional.empty(), other, checkDependencies);
            }
            Self result = this.fuseImpl((Self) other, checkDependencies);
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAction(Stage.END, this, "fuse", Optional.ofNullable(result), other, checkDependencies);
            }
            return result != null ? result : getUndefinedValue();
        } catch (Exception e) {
            throw new ActionException(get_class(), "fuse", e);
        }
    }

    @Override
    public String get_class() {
        return "loop";
    }

    @Override
    public boolean instanceOfImpl(String joinpointClassname) {
        return "loop".equals(joinpointClassname)
            || "stmt".equals(joinpointClassname)
            || "node".equals(joinpointClassname)
            || "joinpoint".equals(joinpointClassname);
    }

    @SuppressWarnings("unchecked")
    @Override
    protected IntFunction<Self[]> selfTypeArrayFactory() {
        return size -> (Self[]) new ALoop[size];
    }
}
