package edge.pkg.abstracts.joinpoints;

import org.lara.interpreter.weaver.interf.*;
import edge.pkg.EdgeWeaver;
import edge.pkg.joinpoints.EdgeLevel2;
import java.lang.Object;
import org.lara.interpreter.exception.AttributeException;
import org.lara.interpreter.weaver.interf.events.Stage;
import java.util.Optional;

import java.util.*;
import java.util.function.IntFunction;

/**
 * Auto-generated abstract class for the {@code reservedKeyword} join point.
 * <p>
 * This class is generated by WeaverGen2 and should not be edited manually.
 */
public abstract class AReservedKeyword<Self extends AReservedKeyword<Self>> extends EdgeLevel2<Self> {

    public AReservedKeyword(Object node, EdgeWeaver weaver) {
        super(node, weaver);
    }

    public abstract String getClassImpl();

    @Deprecated
    public final String _class() {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAttribute(Stage.BEGIN, this, "class", Optional.empty());
            }
            String result = this.getClassImpl();
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAttribute(Stage.END, this, "class", Optional.ofNullable(result));
            }
            return result;
        } catch (Exception e) {
            throw new AttributeException(get_class(), "class", e);
        }
    }

    @Override
    public String get_class() {
        return "reservedKeyword";
    }

    @Override
    public boolean getInstanceOfImpl(String joinpointClassname) {
        return "reservedKeyword".equals(joinpointClassname)
            || "level2".equals(joinpointClassname)
            || "level1".equals(joinpointClassname)
            || "base".equals(joinpointClassname)
            || "joinpoint".equals(joinpointClassname);
    }

    @SuppressWarnings("unchecked")
    @Override
    protected IntFunction<Self[]> selfTypeArrayFactory() {
        return size -> (Self[]) new AReservedKeyword[size];
    }
}
