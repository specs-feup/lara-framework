package edge.pkg.abstracts.joinpoints;

import org.lara.interpreter.weaver.interf.*;
import edge.pkg.EdgeWeaver;
import edge.pkg.joinpoints.EdgeLevel1;
import java.lang.Object;
import org.lara.interpreter.exception.AttributeException;
import org.lara.interpreter.weaver.interf.events.Stage;
import java.util.Optional;

import java.util.*;
import java.util.function.IntFunction;

/**
 * Auto-generated abstract class for the {@code level2} join point.
 * <p>
 * This class is generated by WeaverGen2 and should not be edited manually.
 */
public abstract class ALevel2<Self extends ALevel2<Self>> extends EdgeLevel1<Self> {

    public ALevel2(Object node, EdgeWeaver weaver) {
        super(node, weaver);
    }

    public abstract Integer getCImpl();

    @Deprecated
    public final Object c() {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAttribute(Stage.BEGIN, this, "c", Optional.empty());
            }
            Integer result = this.getCImpl();
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAttribute(Stage.END, this, "c", Optional.ofNullable(result));
            }
            return result;
        } catch (Exception e) {
            throw new AttributeException(get_class(), "c", e);
        }
    }

    @Override
    public String get_class() {
        return "level2";
    }

    @Override
    public boolean getInstanceOfImpl(String joinpointClassname) {
        return "level2".equals(joinpointClassname)
            || "level1".equals(joinpointClassname)
            || "base".equals(joinpointClassname)
            || "joinpoint".equals(joinpointClassname);
    }

    @SuppressWarnings("unchecked")
    @Override
    protected IntFunction<Self[]> selfTypeArrayFactory() {
        return size -> (Self[]) new ALevel2[size];
    }
}
