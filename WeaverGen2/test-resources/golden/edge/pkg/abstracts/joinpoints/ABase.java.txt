package edge.pkg.abstracts.joinpoints;

import org.lara.interpreter.weaver.interf.*;
import edge.pkg.EdgeWeaver;
import edge.pkg.joinpoints.EdgeJoinpoint;
import java.lang.Object;
import org.lara.interpreter.exception.AttributeException;
import org.lara.interpreter.weaver.interf.events.Stage;
import java.util.Optional;

import java.util.*;
import java.util.function.IntFunction;

/**
 * Auto-generated abstract class for the {@code base} join point.
 * <p>
 * This class is generated by WeaverGen2 and should not be edited manually.
 */
public abstract class ABase<Self extends ABase<Self>> extends EdgeJoinpoint<Self> {

    public ABase(Object node, EdgeWeaver weaver) {
        super(node, weaver);
    }

    public abstract Integer getAImpl();

    @Deprecated
    public final Object a() {
        try {
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAttribute(Stage.BEGIN, this, "a", Optional.empty());
            }
            Integer result = this.getAImpl();
            if(getWeaverEngine().hasListeners()) {
                getWeaverEngine().getEventTrigger().triggerAttribute(Stage.END, this, "a", Optional.ofNullable(result));
            }
            return result != null ? result : getUndefinedValue();
        } catch (Exception e) {
            throw new AttributeException(get_class(), "a", e);
        }
    }

    @Override
    public String get_class() {
        return "base";
    }

    @Override
    public boolean instanceOfImpl(String joinpointClassname) {
        return "base".equals(joinpointClassname)
            || "joinpoint".equals(joinpointClassname);
    }

    @SuppressWarnings("unchecked")
    @Override
    protected IntFunction<Self[]> selfTypeArrayFactory() {
        return size -> (Self[]) new ABase[size];
    }
}
