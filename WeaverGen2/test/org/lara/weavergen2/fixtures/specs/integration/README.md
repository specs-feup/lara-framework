# large-integration scenario

This scenario is the integration-test input for `LargeLangSpecIntegrationTest`.

Current baseline is bootstrapped from the `clava` langspec.
You can regenerate the baselines:

`gradle regenLargeIntegrationBaselines`
